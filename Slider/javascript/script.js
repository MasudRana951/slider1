$(document).ready(function(){
    $('.slider_start').slick({
    autoplay: true,
    arrows: false,
    dots: true,
    // prevArrow: <button type="button" class="slick-prev">Previous</button>,
    });
  });